# Dependencies
node_modules

# Build and framework output
.svelte-kit
build
.output
.vercel
.netlify
.wrangler

# Test output
coverage
playwright-report
test-results

# Env
.env
.env.*
!.env.example
!.env.e2e.example

# Logs
*.log

# OS / editor
.DS_Store
Thumbs.db

# Local Claude config, except checked-in skills (see .claude/skills/add-chain).
# Do not replace this with a bare `.claude` ignore: git will not descend into an
# excluded directory, so the negation below would stop working.
.claude/*
!.claude/skills/
